#!/usr/bin/env bash
# Sin compuerta verde no hay commit (AUD2-022). Se activa una vez por clon:
#   git config core.hooksPath .githooks
# Compuerta COMPLETA en cada commit (regla de Walter, 2026-09-24): clippy,
# auditorías de dependencias y prueba de humo del exe real (con SENTRA cerrada).
CLIPPY=1 AUDIT=1 HUMO=1 exec bash "$(git rev-parse --show-toplevel)/scripts/compuerta.sh"
