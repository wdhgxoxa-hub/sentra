"""
Juez de nichos (F3)
===================

El LLM etiqueta evidencia; el juez es código determinista que decide con
reglas explícitas. El LLM nunca emite el veredicto.

Etapas: 0 filtro de calidad (`quality`), 1 etiquetado (`labels`),
2 agrupación (`clustering`), 3 dimensiones (`dimensions`), 4 compuertas y
veredicto (`gates`), 5 abogado del diablo (`advocate`).
"""
