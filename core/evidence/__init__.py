"""
Evidencia multifuente (F2)
==========================

`model` define `EvidenceItem` y `SearchQuery`, el contrato común de todas
las fuentes; `author` convierte al autor en un hash salado (R9).
"""
