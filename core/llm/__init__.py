"""
Motor de IA (F1)
================

`base` define la interfaz `LLMProvider` y lo común a cualquier proveedor
(errores, contabilidad de uso). `gemini` es la única implementación y el
único módulo del proyecto que importa el SDK de Google.
"""
