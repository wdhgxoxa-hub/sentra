# SENTRA - dependencias SOLO de pruebas
# Instalar ademas de requirements.txt: pip install -r requirements-dev.txt

# Lee los PDF generados para comprobar su texto real (tests/test_pdf_export.py).
pypdf==6.19.0

# Linter y comprobador de tipos: su configuración está en pyproject.toml y
# el código propio se mantiene a cero avisos con ellos (AUD-023, AUD-024).
ruff==0.16.6
mypy==2.3.1

# Cliente CDP de la prueba de humo del ejecutable real (tests/humo_exe.py, AUD2-004).
websocket-client==1.9.2

# Auditoría de vulnerabilidades de las dependencias de Python (AUD2-021, DP7 A).
pip-audit==2.10.1
